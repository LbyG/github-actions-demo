name: GitHub Actions Build and Deploy Demo
on:
  push:
    branches:
      - master
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: lsssss
  job2:
    runs-on: ubuntu-latest
    needs: [job1]
    if: success()
    steps:
      - run: lsssss
