name: GitHub Actions Build and Deploy Demo
on:
  push:
    branches:
      - master
jobs:
  job1:
    runs-on: ubuntu-latest
    env:
      NAME: ${{ vars.MY_NAME }}
    steps:
    - name: vars print
      run: echo "NAME=$NAME"
