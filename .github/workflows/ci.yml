name: GitHub Actions Build and Deploy Demo
on:
  push:
    branches:
      - master
jobs:
  job1:
    runs-on: ubuntu-latest
    timeout-minutes: "${{ 'xxxx' }}"
    steps:
      - name: step1
        if: always()
        run: sleep 120s
      - name: step2
        if: cancelled()
        run: sleep 120s
      - name: step3
        if: always()
        run: sleep 120s
