name: GitHub Actions Build and Deploy Demo
on:
  push:
    branches:
      - master
jobs:
  job1:
    env:
      TEST: false
    runs-on: ubuntu-${{ 'latest' }}
    steps:
      - name: step1
        run: ls
      - name: step2
        if: env.TEST
        run: ls
      - name: step3
        run: ls
